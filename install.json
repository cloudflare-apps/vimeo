{
  "resources": {
    "head": [
      {
        "type": "script",
        "src": "./app.js"
      }
    ]
  },
  "preview": {
    "handlers": [
      {
        "options": ["*"],
        "execute": "INSTALL_SCOPE.setOptions(INSTALL_OPTIONS)"
      }
    ]
  },
  "options": {
    "properties": {
      "embeds": {
        "title": "Videos or Playlists",
        "type": "array",
        "items": {
          "title": "Embed",
          "type": "object",
          "properties": {
            "url": {
              "title": "URL",
              "type": "string",
              "description": "The URL of the Vimeo video or playlist to be embedded",
              "order": 1,
              "default": "https://vimeo.com/45196609",
              "placeholder": "https://vimeo.com"
            },
            "location": {
              "title": "Location",
              "type": "object",
              "format": "element",
              "description": "Where should the video appear on the page?",
              "order": 2,
              "default": {
                "selector": "body",
                "method": "prepend"
              }
            },
            "autoplay": {
              "title": "Autoplay",
              "type": "boolean",
              "description": "Should the video play automatically when the page is loaded?",
              "order": 3
            }
          }
        }
      }
    }
  }
}